export const requestedMessage = (msg) => {
  return `${msg}_REQUESTED`;
};

export const successMessage = (msg) => {
  return `${msg}_SUCCESS`;
};

export const failedMessage = (msg) => {
  return `${msg}_FAILED`;
};
